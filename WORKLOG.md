# 더파인드핫딜 작업 기록

다른 작업자와 이어서 진행할 수 있도록, 기능 변경과 운영 결정을 날짜순으로 기록한다.
비밀번호, API 키, 토큰 등 비밀값은 이 문서에 절대 적지 않는다.

## 2026-09-06 — 공개 관리자 링크 제거 및 메인 화면 개선

### 완료

- 공개 헤더와 푸터에서 `/admin` 링크를 제거했다.
- `/admin` 주소와 관리자 로그인 기능은 유지한다. 운영자는 북마크나 주소 직접 입력으로 접속한다.
- Cloudflare Workers의 `ADMIN_PASSWORD` 비밀값이 등록된 것을 이름만 확인했다.
- 코드의 기본 관리자 비밀번호를 제거했다. 환경 변수 누락 시 관리자 로그인과 세션 검증은 모두 실패하도록 변경했다.
- 메인 첫 화면에 `오늘의 핫딜` 영역을 추가했다.
  - 판매가가 있는 상품 중 할인율 우선, 상품 순위 보조 기준으로 최대 3개를 노출한다.
  - 카테고리 필터를 선택하면 이 추천 영역은 숨기고 목록 탐색에 집중한다.

### 검증 및 배포

- Workers용 프로덕션 빌드 성공: `npm run build:vinext`
- 대상 파일 ESLint 검사: 오류 없음. 이미지 태그 관련 기존 경고만 남아 있다.
- GitHub `main` 푸시 완료: 커밋 `eb59a2c` (`관리자 접근 정리 및 오늘의 핫딜 추가`)
- Cloudflare Workers 배포 완료
  - 운영 URL: `https://hotdeal-site.sxc4566.workers.dev`
  - 배포 후 공개 관리자 링크 제거와 `오늘의 핫딜` 노출을 확인했다.

## 운영 기준 — 토스 API 호출은 Oracle 수집기에서만 실행

### 문제

- 관리자 화면의 `베스트 불러오기`와 `전부 불러오기`는 Cloudflare Workers에서 토스 쉐어링크 API를 직접 호출한다.
- 토스는 출발 IP 화이트리스트를 적용하고 있어 Cloudflare의 가변 출발 IP 요청을 차단한다.
- 배포 사이트(`workers.dev`)의 관리자 화면에서 해당 버튼을 누르면 차단 오류가 발생한다.

### 현재 운영 구조

오라클 클라우드 서버가 이미 토스 API 전용 고정 IP 수집기로 운영 중이다. 웹사이트는 Cloudflare Workers가 서빙한다.

```text
Oracle systemd timer (2시간마다)
  → collect-to-kv.mjs
  → 토스 베스트 30개 조회 및 신규 상품 쉐어링크 발급
  → Cloudflare KV 저장
  → 홈페이지 표시 및 텔레그램 알림
```

- 수집기는 가격 이력(`toss:ph`)도 매일 기록한다.
- 오라클 수집기 공용 IPv4는 토스 화이트리스트에 이미 등록돼 있다. 실제 IP 값은 서버 접속 기록 또는 토스 관리자에서 확인한다.
- `systemd timer`가 2시간마다 실행하므로 일반적인 갱신은 수동 실행이 필요 없다.

### 관리자 사용 규칙

- 배포 사이트는 읽기 전용으로 사용한다.
- 글 등록과 `베스트 불러오기`/`전부 불러오기` 같은 토스 API 동작은 로컬 관리자(`http://127.0.0.1:3000/admin`)에서만 실행한다.
- 로컬 환경의 출발 IP는 토스 화이트리스트 등록 상태여서 수동 불러오기가 가능하다.
- 오라클 수집기 점검은 SSH로 접속한 뒤 `hotdeal-collector` 서비스와 타이머 상태, journal 로그를 확인한다.

### 로컬 컴퓨터 전원 및 터미널

- 자동 수집은 오라클 서버의 `systemd timer`가 담당하므로, 로컬 컴퓨터를 종료해도 정기 수집과 공개 사이트 운영은 계속된다.
- 2026-09-06 점검 시 로컬의 실행 중인 Node 프로세스 중 이 사이트의 일반 개발 포트(3000/3001)를 열고 있는 프로세스는 없었다.
- 로컬 터미널은 수동 등록·즉시 수집·개발용일 수 있으므로, 실행 중인 명령이 필요한 작업인지 모를 때는 종료하지 말고 창의 명령 내용을 먼저 확인한다.

### 향후 개선 후보

- 배포 관리자 화면에서는 토스 자동불러오기 버튼을 숨기거나 비활성화해 운영 실수를 예방한다.
- 로컬 관리자에서는 현재 수동 등록과 자동불러오기 기능을 유지한다.

## 검토 — 현재 접속자 표시

- 홈페이지에 `지금 접속 중 N명` 형태의 표시를 추가할 수 있다.
- 정확한 동시 접속 수는 Cloudflare Durable Object와 WebSocket 연결로 관리한다. 단순 방문 통계나 KV 목록으로는 실시간 정확도를 보장하지 않는다.
- 이 수치는 사람 수가 아니라 현재 사이트를 열어 둔 브라우저 탭/연결 수다. 개인정보나 로그인 정보는 저장하지 않는다.
- 접속자가 없는 동안에도 비용을 낮추기 위해 Durable Object WebSocket Hibernation 방식을 사용한다.

## 기록 규칙

- 작업을 시작할 때: 목표와 전제 조건을 한두 줄로 추가한다.
- 작업을 마칠 때: 수정 파일, 검증 결과, 배포/커밋 여부를 추가한다.
- 중요한 운영·보안 결정은 이유와 되돌리는 방법을 함께 기록한다.
